{% extends 'GlavBundle::layoutAdmin.html.twig' %}

{% block infoUserTop %}
    <h1>Automotor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Hash</th>
                <th>Tipo_rubro</th>
                <th>Modelo</th>
                <th>Matricula</th>
                <th>Estado</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('automotor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.hash }}</td>
                <td>{{ entity.tiporubro }}</td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.matricula }}</td>
                <td>{{ entity.estado }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('automotor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('automotor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('automotor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
