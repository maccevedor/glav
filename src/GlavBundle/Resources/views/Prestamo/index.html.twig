{% extends 'GlavBundle::layoutAdmin.html.twig' %}

{% block infoUserTop  %}
    <h1>Prestamo list</h1>

    <table class="table table-striped table-bordered bootstrap-datatable datatable">



        <thead>
            <tr>
                <th>Id</th>
                <th>Valor</th>
                <th>Observacion</th>
                <th>Estado</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('prestamo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.valor }}</td>
                <td>{{ entity.observacion }}</td>
                <td>{{ entity.estado }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('prestamo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('prestamo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('prestamo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
