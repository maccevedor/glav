{% extends 'GlavBundle::layoutAdmin.html.twig' %}

{% block infoUserTop %}
    <h1>Editar Tipo de Automotor</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoautomotor') }}">
            Volver a la Lista Tipo de Automotor
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
