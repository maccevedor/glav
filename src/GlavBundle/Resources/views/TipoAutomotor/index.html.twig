{% extends 'GlavBundle::layoutAdmin.html.twig' %}

{% block infoUserTop %}
    <h1>Tipo de Automotor</h1>
<ul>
        <li>
            <a href="{{ path('tipoautomotor_new') }}">
                Crear Tipo de Automotor
            </a>
        </li>
    </ul>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
               
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Estado</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipoautomotor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.estado }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <a class="btn btn-success" href="{{ path('tipoautomotor_show', { 'id': entity.id }) }}">
                <i class="halflings-icon white zoom-in"></i>  
                </a>
                <a class="btn btn-info" href="{{ path('tipoautomotor_edit', { 'id': entity.id }) }}">
                <i class="halflings-icon white edit"></i>  
                </a>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipoautomotor_new') }}">
                Crear Tipo de Automotor
            </a>
        </li>
    </ul>
    {% endblock %}
