{% extends 'GlavBundle::layoutAdmin.html.twig' %}

{% block infoUserTop %}
    <h1>Editar Cliente</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cliente') }}">
            Volver a la Lista Cliente
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
